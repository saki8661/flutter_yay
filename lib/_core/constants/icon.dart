import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_blog/_core/constants/color.dart';

Icon iconArrowBack({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.arrow_back_ios, color: mColor, size: mSize);
}

Icon iconArrowForward({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.arrow_forward_ios, color: mColor, size: mSize);
}

Icon iconWriteMenu({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.edit_note, color: mColor, size: mSize);
}

Icon iconEmptyStar({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.star_border_rounded, color: mColor, size: mSize);
}

Icon iconFullStar({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.star_rounded, color: mColor, size: mSize);
}

Icon iconEmptyHeart({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.favorite_border_rounded, color: mColor, size: mSize);
}

Icon iconFullHeart({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.favorite_rounded, color: mColor, size: mSize);
}

Icon iconPost({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.edit_outlined, color: mColor, size: mSize);
}

Icon iconReply({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.comment_outlined, color: mColor, size: mSize);
}

Icon iconDotMenu({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.more_vert_sharp, color: mColor, size: mSize);
}

Icon iconPriceCheck({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.price_check, color: mColor, size: mSize);
}

Icon iconRefund({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(CupertinoIcons.gobackward, color: mColor, size: mSize);
}

Icon iconHamburgerMenu({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.density_medium_rounded, color: mColor, size: mSize);
}

Icon iconSearch({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(CupertinoIcons.search, color: mColor, size: mSize);
}

Icon iconPhoto({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.photo_camera, color: mColor, size: mSize);
}

Icon iconClose({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.close, color: mColor, size: mSize);
}

Icon iconPause({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.pause, color: mColor, size: mSize);
}

Icon iconBookMark({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.bookmark, color: mColor, size: mSize);
}

Icon iconBookMartOutline({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.bookmark_border_outlined, color: mColor, size: mSize);
}

Icon iconPhone({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.phone_iphone, color: mColor, size: mSize);
}

Icon iconBottomSearch({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.manage_search, color: mColor, size: mSize);
}

Icon iconUnchanged({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.remove, color: mColor, size: mSize);
}

Icon iconUp({Color mColor = kFontRed, double mSize = 25}) {
  return Icon(Icons.arrow_drop_up, color: mColor, size: mSize);
}

Icon iconDown({Color mColor = kBlue, double mSize = 25}) {
  return Icon(Icons.arrow_drop_down, color: mColor, size: mSize);
}

Icon iconBottomSetting({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(CupertinoIcons.profile_circled, color: mColor, size: mSize);
}

Icon iconBottomlibrary({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.shelves, color: mColor, size: mSize);
}

Icon iconBottomFeed({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(CupertinoIcons.today, color: mColor, size: mSize);
}

// TODO : 바텀네비게이션 - 투데이 아이콘 결정하기
Icon iconBottomToday({Color mColor = kFontBlack, double mSize = 25}) {
  return Icon(Icons.accessibility, color: mColor, size: mSize);
}
